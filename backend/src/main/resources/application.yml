server:
  port: 8080

spring:
  application:
    name: algong-backend

  datasource:
    url: jdbc:mysql://localhost:3306/algong?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: algong
    password: algong0302
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
